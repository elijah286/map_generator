{
  "name": "map-gen-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prestart": "lsof -ti :${PORT:-3847} | xargs kill -9 2>/dev/null || true",
    "start": "node server.js",
    "predev": "lsof -ti :${PORT:-3847} | xargs kill -9 2>/dev/null || true",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "d3-geo": "^3.1.1",
    "dotenv": "^17.4.1",
    "express": "^5.2.1",
    "multer": "^2.1.1",
    "openai": "^6.33.0",
    "topojson-client": "^3.1.0",
    "world-atlas": "^2.0.2",
    "xlsx": "^0.18.5"
  }
}
